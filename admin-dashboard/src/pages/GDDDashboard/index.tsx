import React from 'react';
import { CommandCenterLayout } from './CommandCenterLayout';

/**
 * Renders the GDD dashboard by composing the CommandCenterLayout component.
 *
 * @returns The JSX element for the GDD dashboard (renders `CommandCenterLayout`).
 */
function GDDDashboard() {
  return <CommandCenterLayout />;
}

export default GDDDashboard;